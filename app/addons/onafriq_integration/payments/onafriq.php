<?php

/**
 * @var array $order_info
 * @var array $processor_data
 */
defined('BOOTSTRAP') or die('Access denied');

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    if ($mode === 'standard_notification') {

    }
}

/**
 * @var array $order_info
 * @var array $processor_data
 */
defined('BOOTSTRAP') or die('Access denied');

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    if ($mode === 'standard_notification') {

    }
}else{
    //TODO: Add else
}